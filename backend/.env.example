# Database
# Local development (requires PostgreSQL installed locally)
DATABASE_URL="postgresql://routeiq:password@localhost:5432/routeiq_dev"

# Supabase production - Direct connection (for local development/migrations)
# DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-us-east-1.pooler.supabase.com:5432/postgres"

# Supabase production - Transaction Pooling (for deployed backend on Koyeb)
# DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Auth
JWT_SECRET="your-super-secret-key-change-in-production-min-32-chars"
JWT_EXPIRES_IN="7d"
BCRYPT_ROUNDS=10

# API
PORT=3001
NODE_ENV=development
CORS_ORIGIN="http://localhost:3000"

# External APIs (Phase 2+)
STRAVA_CLIENT_ID=""
STRAVA_CLIENT_SECRET=""
OPENWEATHER_API_KEY=""
